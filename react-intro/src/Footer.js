import React from "react";
import PropTypes from "prop-types";

const Footer = (props) => {
  return (
    <footer>
      <p>Powered by REACT</p>
    </footer>
  );
};

Footer.propTypes = {};

export default Footer;
