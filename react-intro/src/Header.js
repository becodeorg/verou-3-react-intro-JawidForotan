import React from "react";
import PropTypes from "prop-types";

const Header = (props) => {
  return (
    <div>
      <nav>
        <h1>ToDo APP</h1>
      </nav>
    </div>
  );
};

Header.propTypes = {};

export default Header;
