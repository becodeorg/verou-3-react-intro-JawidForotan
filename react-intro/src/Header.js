import React from "react";
import PropTypes from "prop-types";

const Header = (props) => {
  return (
    <div>
      <nav>
        <a href="">Home</a>
        <a href="">About</a>
        <a href="">Info</a>
      </nav>
    </div>
  );
};

Header.propTypes = {};

export default Header;
